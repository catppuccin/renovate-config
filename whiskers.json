{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "customManagers": [
    {
      "customType": "regex",
      "fileMatch": [
        "^.*.tera$"
      ],
      "matchStrings": [
        "( *)?version: (?<currentValue>.*?)\n"
      ],
      "depNameTemplate": "catppuccin/mdbook",
      "extractVersionTemplate": "^v?(?<version>.*)$",
      "datasourceTemplate": "github-tags"
    }
  ]
}
